# План устранения технических рисков

## Цель

Снизить риск расхождения desktop-версии и расширения VS Code, стабилизировать тесты и выпуск версий, а затем безопасно разделить крупные модули без одновременной переработки всего проекта.

## Проверенное текущее состояние

- Полный `npm test` проходит успешно.
- Недетерминированность теста поисковика была вызвана влиянием системного `LANG`: тест ожидал DuckDuckGo, но не передавал locale явно. Тест теперь передаёт `{ locale: "en" }`.
- Desktop, расширение VS Code и плагин PyCharm синхронизированы на версии `0.4.25`.
- `.gitignore` уже содержит `.DS_Store` и `*.vsix`.
- Находящиеся локально `.vsix` и `.DS_Store` не отслеживаются Git. Удаление их из истории сейчас не требуется; при необходимости очистка старой истории должна выполняться отдельной согласованной операцией.

## Принципы выполнения

1. Один этап — один отдельный коммит или pull request.
2. Перед каждым этапом фиксировать исходный `git status` и запускать полный `npm test`.
3. Не переносить одновременно UI, сервер и API.
4. Сначала добавлять архитектурные проверки, затем менять структуру.
5. После каждого переноса сохранять обратную совместимость импортов либо менять все импорты в одном атомарном коммите.

## Этап 0. Стабилизация тестов

**Приоритет:** высокий
**Размер:** малый
**Риск:** низкий

### Работы

- [x] Воспроизвести падение `test/search-engine.test.mjs`.
- [x] Устранить зависимость теста от системного `LANG`, явно задав locale.
- [x] Запустить отдельный тест поисковика.
- [x] Запустить полный `npm test`.
- [ ] Добавить правило: тесты выбора locale/search engine всегда передают locale или изолируют переменные окружения.
- [ ] Проверить остальные тесты на использование `LANG`, `LC_ALL`, timezone, случайных портов и текущего времени.

### Критерий готовности

Полный набор тестов проходит повторяемо минимум 5 запусков подряд на локальной машине и в CI.

## Этап 1. Безопасность рабочего дерева и документация

**Приоритет:** высокий
**Размер:** малый
**Риск:** низкий

### Работы

- [x] Обновить в `README.md` упоминания `273` тестов после определения фактического стабильного числа.
- [x] Лучше заменить жёстко заданное число тестов на формулировку без счётчика либо генерируемый CI-badge, чтобы документация снова не устарела.
- [x] Принять единый номер релиза для desktop и VS Code.
- [x] Проверить, что локальные `.vsix` и `.DS_Store` не попадают в новые коммиты.

### Решение по версиям

Принятый вариант: **единая версия продуктов**.

- `package.json` и `plugin-for-vscode/package.json` получают одинаковый номер перед выпуском.
- Общий shared-пакет после появления получает собственную версию или остаётся приватной workspace-зависимостью.
- Общий релизный тег соответствует обеим поставкам.

Текущий единый номер desktop, VS Code и PyCharm: `0.4.25`.

### Критерий готовности

README соответствует реальному состоянию, политика версий записана, а рабочее дерево подготовлено для атомарных изменений.

## Этап 2. CI и архитектурные предохранители

**Приоритет:** высокий
**Размер:** средний
**Риск:** низкий

### Минимальный CI

- [x] Установка зависимостей через воспроизводимую команду.
- [x] Запуск `npm test`.
- [x] Проверка синхронизации каталога моделей desktop/VS Code.
- [x] Проверка версий и формата package-файлов.
- [x] Проверка отсутствия отслеживаемых `.DS_Store` и `.vsix`.
- [x] Проверка сборки расширения без публикации.

### Дополнительные проверки

- [ ] Добавить lint/format только после фиксации текущего форматирования, чтобы не создавать огромный нерелевантный diff.
- [ ] Добавить тест импортов shared-модулей.
- [ ] Добавить тест, запрещающий появление новых копий выбранных shared-файлов.

### Критерий готовности

Каждый pull request автоматически проверяет тесты, архитектурные инварианты и сборку расширения.

## Этап 3. Инвентаризация дублирования

**Приоритет:** высокий
**Размер:** средний
**Риск:** низкий

### Работы

- [x] Построить список файлов с одинаковыми относительными путями в `src/` и `plugin-for-vscode/src/`.
- [x] Разделить их на категории:
  - полностью идентичные;
  - отличаются только импортами/путями;
  - намеренно платформенные;
  - случайно разошедшиеся.
- [x] Отдельно сравнить `api/` и `plugin-for-vscode/api/`.
- [x] Для каждого расхождения назначить источник истины.
- [x] Зафиксировать матрицу владения модулей в документации (`docs/DUPLICATE_MODULES_INVENTORY.md`).
- [x] Добавить скрипт инвентаризации `scripts/inventory-duplicates.mjs` и интегрировать в CI.

### Первыми кандидатами для shared

1. Чистые функции без UI и файловой системы.
2. Каталог моделей и provider registry abstractions.
3. Парсеры, нормализаторы и protocol helpers.
4. Memory/search algorithms.
5. Code-agent parser и общие validation helpers.

### Не переносить первыми

- `window-app/server.mjs`;
- browser launch/profile management;
- updater;
- VS Code extension entry point;
- платформенные пути и lifecycle.

### Критерий готовности

Есть проверенная таблица модулей и порядок переноса; ни один файл не переносится только из-за совпадения имени.

## Этап 4. Создание shared/core

**Приоритет:** высокий
**Размер:** крупный, выполнять сериями малых PR
**Риск:** средний

### Предлагаемая структура



### Порядок миграции

1. Создать приватный `packages/core` без переноса поведения.
2. Перенести один небольшой чистый модуль.
3. Перевести desktop и VS Code на один импорт.
4. Удалить только две заменённые копии.
5. Запустить тесты и сборку расширения.
6. Добавить архитектурный тест, запрещающий возврат этих копий.
7. Повторять для следующего модуля.

### Важное ограничение

Не делать массовое перемещение всего `src/`. Это затруднит поиск регрессии и усложнит проверку поведения.

### Критерий готовности

Общий код физически существует в одном месте, а desktop и расширение используют его напрямую.

## Этап 5. Разделение монолитов

**Приоритет:** средний
**Размер:** крупный
**Риск:** средний/высокий

Выполнять только после стабилизации shared/core и CI.

### 5.1 `src/window-app/server.mjs`

- [ ] Зафиксировать текущие маршруты интеграционными тестами.
- [ ] Выделять маршруты по одной предметной области.
- [ ] Передавать зависимости явно: state, providers, browser service, task runner.
- [ ] Оставить `server.mjs` точкой композиции и запуска.

Предлагаемые группы:



### 5.2 `api/openai-handler.mjs`

- [ ] Сначала добавить контрактные тесты OpenAI-compatible API.
- [ ] Выделить validation, model resolution, message normalization и streaming.
- [ ] Не менять формат ошибок и SSE-событий во время структурного переноса.

Предлагаемая структура:



### 5.3 `src/window-app/ui-html.mjs`

- [ ] Зафиксировать DOM hooks и browser-side API тестами.
- [ ] Сначала вынести статические шаблоны и чистые render-функции.
- [ ] Затем вынести клиентские модули по областям: chat, settings, providers, browser, memory.
- [ ] Не менять одновременно разметку, CSS-классы и поведение.

### 5.4 `src/window-app/ui-styles.mjs`

- [ ] Разделять стили вслед за UI-компонентами, а не заранее.
- [ ] Сохранить порядок подключения CSS, чтобы избежать изменений каскада.

### Критерий готовности

Точки входа отвечают только за композицию; маршруты, сервисы и UI-компоненты тестируются изолированно.

## Этап 6. Релизные артефакты

**Приоритет:** средний
**Размер:** малый/средний
**Риск:** низкий для новых релизов, высокий для переписывания истории

### Текущее состояние

`.gitignore` уже игнорирует `*.vsix`, а `git ls-files` не показывает отслеживаемых VSIX. Следовательно, локальные архивы не являются текущей проблемой индекса Git.

### Работы

- [ ] Собирать VSIX в CI.
- [ ] Прикладывать VSIX к релизу, а не коммитить в репозиторий.
- [ ] Настроить retention артефактов CI.
- [ ] Если старые VSIX действительно присутствовали в истории и заметно раздувают clone, отдельно измерить размер и только затем решать вопрос о переписывании истории.

### Критерий готовности

Новые релизы воспроизводимы из тега, а бинарные артефакты не попадают в Git.

## Предлагаемая последовательность pull request

1. **PR-1:** детерминированный search-engine test.
2. **PR-2:** README и документированная политика версий.
3. **PR-3:** базовый CI и проверки мусорных артефактов.
4. **PR-4:** отчёт/скрипт инвентаризации дублирования.
5. **PR-5:** каркас `packages/core` и перенос одного чистого модуля.
6. **PR-6 и далее:** последовательный перенос общих модулей.
7. **Отдельная серия:** декомпозиция server/API/UI, по одной области на PR.
8. **Финальный PR:** CI-релиз VSIX и документация релизного процесса.

## Метрики результата

- Полный тестовый набор стабильно зелёный.
- Количество дублирующихся идентичных модулей уменьшается после каждого PR.
- Shared-модули имеют одну реализацию и используются обоими продуктами.
- Ни один основной entry-point не растёт после начала декомпозиции.
- VSIX и системные файлы не отслеживаются Git.
- Версия каждого продукта определяется из его `package.json`, а правила релиза задокументированы.

## Выполнено в текущем изменении

- README больше не содержит хрупкого фиксированного числа тестов.
- Зафиксирована единая версия desktop и VS Code (`0.4.25`).
- Добавлен GitHub Actions CI: воспроизводимая установка, архитектурные проверки, полный набор тестов и проверка сборки расширения.
- Выполнен Этап 3 (PR-4): создана инвентаризация дублирующихся модулей (`docs/DUPLICATE_MODULES_INVENTORY.md`), добавлен CI-скрипт контроля синхронизации `scripts/inventory-duplicates.mjs` и unit-тест `test/duplicate-inventory.test.mjs`.

## Ближайшее практическое действие

Выполнить Этап 4 (PR-5): создать приватный каркас `packages/core` и перенести первый чистый модуль (`src/providers/model-catalog.mjs` и `src/i18n/`) на единый импорт без изменения поведения.
