# AI Free 0.4.19

AI Free 0.4.19 removes the completed EconomyOS giveaway from the product interface.

## Removed campaign UI

- Removed the automatic startup giveaway dialog.
- Removed the giveaway notification sound.
- Removed the sidebar campaign banner and raffle purchase link.
- Removed the dedicated giveaway configuration and styles from desktop and VS Code.

## Retained fixes

- Agent memory and project instructions remain available in compact follow-up prompts.
- Workspace write-tool recovery remains enabled when a model incorrectly denies tool access.
- ChatGPT composer and login recovery improvements remain active.

## Verification

- Desktop and VS Code versions are synchronized at `0.4.19`.
- Full test suite (456 tests), CI invariants and VS Code packaging passed.
- Release tag: `v0.4.19`.
